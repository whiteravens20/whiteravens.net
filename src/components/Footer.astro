---
import copy from '../content/copy.yml';
import { site } from '../config/site';
---

<footer>
  <div class="wrap inner">
    <a class="kofi" href={site.kofi} rel="noopener noreferrer">
      <img src={site.kofiButton} alt={copy.footer.kofi} height="36" loading="lazy" />
    </a>
    <p class="credit">
      {copy.footer.creditBefore}<span class="heart">❤</span>{copy.footer.creditAfter}<a
        href={site.author.url}
        rel="noopener noreferrer">{site.author.name}</a
      >
    </p>
    <nav class="legal" aria-label={copy.footer.legal}>
      <a href="/privacy/">{copy.footer.privacy}</a>
      <a href="/terms/">{copy.footer.terms}</a>
    </nav>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--border);
    padding-block: var(--space-xl);
  }

  .inner {
    display: grid;
    justify-items: center;
    gap: var(--space-l);
    text-align: center;
  }

  .credit a,
  .legal a {
    color: var(--text-muted);
    text-decoration: none;
    transition: color var(--dur-fast) var(--ease);
  }

  .credit a:hover,
  .legal a:hover {
    color: var(--accent);
  }

  .legal {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-xs) var(--space-l);
    font-size: var(--step--1);
  }

  .kofi img {
    height: 36px;
    width: auto;
  }

  .credit {
    font-size: var(--step--1);
    color: var(--text-muted);
  }

  .heart {
    color: var(--accent-2);
  }
</style>
